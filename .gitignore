# Deployment bundles built with `npm run build`. Generated output, not source:
# reproducible any time, ~28MB each, and stale the moment anything changes.
*-deploy.zip

node_modules/
.next/
out/
build/
.env
.env.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
/.data/
coverage/
